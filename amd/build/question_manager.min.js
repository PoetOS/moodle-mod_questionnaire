/**
 * Drag-and-drop reordering for the manage-questions page.
 *
 * Wires core/sortable_list onto the question list; on drop, persists the new order via
 * the mod_questionnaire_question_reorder AJAX external. The server is the source of truth
 * for whether an order is valid (a question cannot be dropped at or before one it depends
 * on) — sortable_list has no way to block an individual drop target, so a rejected drop
 * shows the server's error and reloads to restore the true order. A drop that is accepted
 * but triggers a page-break repair also reloads, so position numbers never lie.
 *
 * Keyboard reordering comes for free from core/drag_handle + core/sortable_list's built-in
 * move dialogue; no separate up/down control is needed.
 *
 * @module     mod_questionnaire/question_manager
 * @copyright  2026 Mike Churchward (mike.churchward@poetopensource.org)
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("mod_questionnaire/question_manager",["core/sortable_list","core/ajax","core/notification","core/str","core/toast"],(function(SortableList,Ajax,Notification,Str,Toast){var SELECTORS_list='[data-region="question-list"]',SELECTORS_row="[data-question-id]",SELECTORS_positionBadge='[data-region="position-badge"]';return{init:function(cmId){var listElement=document.querySelector(SELECTORS_list);listElement&&(new SortableList(listElement).getElementName=function(element){return Promise.resolve(element[0].getAttribute("data-name"))},document.addEventListener(SortableList.EVENTS.elementDrop,(function(event){if(event.detail.positionChanged){var itemOrder=function(listElement){var ids=[];return listElement.querySelectorAll(SELECTORS_row).forEach((function(row){ids.push(row.getAttribute("data-question-id"))})),ids.join(",")}(listElement);Ajax.call([{methodname:"mod_questionnaire_question_reorder",args:{cmid:cmId,itemorder:itemOrder}}])[0].then((function(result){return result.reload?(Notification.addNotification({message:result.warnings,type:"warning"}),window.location.reload(),null):(function(listElement){listElement.querySelectorAll(SELECTORS_row).forEach((function(row,index){var badge=row.querySelector(SELECTORS_positionBadge);badge&&(badge.textContent=index+1)}))}(listElement),Str.get_string("questionmoved","mod_questionnaire"))})).then((function(string){return string&&Toast.add(string),null})).catch((function(error){Notification.exception(error),window.location.reload()}))}})))}}}));

//# sourceMappingURL=question_manager.min.js.map